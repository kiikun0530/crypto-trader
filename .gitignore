# APIキーを含むファイル（セキュリティのため）
# coincheck_balance.py

# 出力ファイル
balance_data.json
response.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
.venv

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
tfplan
terraform.tfvars
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Logs
*.log
logs/

# Secrets / API Keys
.env
.env.*
!.env.example
*.pem
*.key

# ONNX models (migrated to SageMaker Chronos-2)
models/chronos-onnx/

# Lambda Layer build artifacts
onnx_layer/
onnx_layer.zip
terraform/*.out

terraform/*-output.json
terraform/response.json

# Terraform temp files
tf_state.txt

# Lambda deployment zips
*.zip

# Data analysis (local dumps & ad-hoc scripts)
data/

# Debug / test output JSON
*-out.json
out.json
sf-history.json
warmup-output.json

# Lambda layer pip packages (keep requirements.txt & trading_common.py only)
lambda_layer/python/*
!lambda_layer/python/requirements.txt
!lambda_layer/python/trading_common.py
