name: Post to X on Push

on:
  push:
    branches: [main]

jobs:
  post-to-x:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests requests-oauthlib

      - name: Generate tweet and post to X
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_SECRET: ${{ secrets.X_ACCESS_SECRET }}
        run: |
          python << 'SCRIPT'
          import os
          import subprocess
          import requests
          from requests_oauthlib import OAuth1

          # コミット情報取得（件名 + 本文）
          commit_subject = subprocess.check_output(
              ['git', 'log', '-1', '--pretty=format:%s'],
              text=True
          ).strip()
          commit_body = subprocess.check_output(
              ['git', 'log', '-1', '--pretty=format:%b'],
              text=True
          ).strip()
          commit_sha = subprocess.check_output(
              ['git', 'log', '-1', '--pretty=format:%h'],
              text=True
          ).strip()

          # 差分情報（変更ファイル・行数）
          try:
              diff_stat = subprocess.check_output(
                  ['git', 'diff', '--stat', 'HEAD~1', 'HEAD'],
                  text=True
              ).strip()
          except Exception:
              diff_stat = "(diff not available)"

          # 変更ファイル名一覧
          try:
              changed_files = subprocess.check_output(
                  ['git', 'diff', '--name-only', 'HEAD~1', 'HEAD'],
                  text=True
              ).strip()
          except Exception:
              changed_files = ""

          print(f"Commit: {commit_sha}")
          print(f"Subject: {commit_subject}")
          print(f"Body: {commit_body[:200]}")
          print(f"Changed files: {changed_files[:300]}")

          # GitHub Models APIでX投稿文を生成
          github_token = os.environ.get('GITHUB_TOKEN', '')
          tweet = None

          SYSTEM_PROMPT = """あなたはフリーランスエンジニアの個人開発日記をX（Twitter）に投稿する代筆者です。

          【あなたの役割】
          コミットログと差分情報を読み取って、「開発者本人が書いたかのような」自然なX投稿を生成する。
          単なる更新報告ではなく、開発の裏側にあるストーリーを伝える。

          【投稿で伝えるべきこと（以下から1-2個選ぶ）】
          - なぜこの変更をしたのか（背景・動機）
          - どんな技術的チャレンジがあったか（苦労話）
          - 何を学んだか・発見したか
          - どんなアーキテクチャ判断をしたか（トレードオフ）
          - 数字で語れる成果（コスト削減額、パフォーマンス改善等）

          【トーンとスタイル】
          - 日本語で書く
          - 280文字以内（厳守）
          - 一人称は使わないか「自分」。「私」は使わない
          - 等身大で素直。技術への真剣さが伝わるトーン
          - 絵文字は0-2個（なくてもいい）
          - 技術用語は正確に使う（採用担当よりエンジニアに刺さる内容を優先）
          - 大げさ・煽り・自画自賛は絶対NG
          - 「〜しました！」の羅列にしない

          【必須要素】
          - リポジトリURL: https://github.com/kiikun0530/crypto-trader
          - ハッシュタグ 2-3個（末尾に）: #個人開発 #AWS #Python #Terraform #フリーランスエンジニア #サーバーレス から選択

          【良い投稿例】
          「SageMakerのクォータ承認が数日かかりそうだったので、Chronos-T5-TinyをONNXに変換してLambda直接実行に切り替えた。結果的にSageMaker不要でコスト$8/月削減。制約が良い設計を生むこともある https://github.com/kiikun0530/crypto-trader #個人開発 #AWS」

          「6通貨同時分析するとLambda 18並列になるけど、DynamoDBのオンデマンドなら全然耐えられる。月$0.30。サーバーレスのコスト感覚がだいぶ掴めてきた https://github.com/kiikun0530/crypto-trader #個人開発 #サーバーレス」

          「価格変動の検知窓を±60秒→±300秒に広げただけで、5分足のキャンドル境界問題が解決。地味だけどこういうバグが一番つらい https://github.com/kiikun0530/crypto-trader #個人開発 #Python」

          【悪い例（避けるべき）】
          - 「crypto-trader更新: XXXを追加しました #個人開発」← 更新報告でしかない
          - 「🚀🔥すごいの作った！！」← 煽り
          - 「feat: add ONNX support」← コミットログそのまま"""

          # LLMに渡すユーザーメッセージ（リッチなコンテキスト）
          body_text = commit_body if commit_body else '(なし)'
          user_message = (
              "以下のコミット情報からX投稿を生成してください。\n"
              "コミットログだけでなく、変更ファイルや差分から「何をなぜやったか」を読み取って、開発者の視点で語ってください。\n\n"
              "## コミット\n"
              f"件名: {commit_subject}\n"
              "本文:\n"
              f"{body_text}\n\n"
              "## 変更ファイル\n"
              f"{changed_files}\n\n"
              "## 差分サマリ\n"
              f"{diff_stat}"
          )

          try:
              resp = requests.post(
                  'https://models.inference.ai.azure.com/chat/completions',
                  headers={
                      'Authorization': f'Bearer {github_token}',
                      'Content-Type': 'application/json'
                  },
                  json={
                      'model': 'gpt-4o-mini',
                      'messages': [
                          {
                              'role': 'system',
                              'content': SYSTEM_PROMPT
                          },
                          {
                              'role': 'user',
                              'content': user_message
                          }
                      ],
                      'max_tokens': 350,
                      'temperature': 0.7
                  },
                  timeout=30
              )
              if resp.status_code == 200:
                  data = resp.json()
                  tweet = data['choices'][0]['message']['content'].strip()
                  # 余分なクォートを除去
                  if tweet.startswith('"') and tweet.endswith('"'):
                      tweet = tweet[1:-1]
                  if tweet.startswith("'") and tweet.endswith("'"):
                      tweet = tweet[1:-1]
                  print(f"Generated tweet: {tweet}")
              else:
                  print(f"GitHub Models failed: {resp.status_code} {resp.text}")
          except Exception as e:
              print(f"GitHub Models failed: {e}")

          # フォールバック
          if not tweet:
              # コミット本文があれば使う、なければ件名を使う
              detail = commit_body.split('\n')[0][:80] if commit_body else commit_subject[:80]
              tweet = (
                  f"{detail}\n"
                  f"https://github.com/kiikun0530/crypto-trader\n"
                  f"#個人開発 #AWS #フリーランスエンジニア"
              )
              print(f"Using fallback: {tweet}")

          # X API投稿
          api_key = os.environ.get('X_API_KEY', '')
          api_secret = os.environ.get('X_API_SECRET', '')
          access_token = os.environ.get('X_ACCESS_TOKEN', '')
          access_secret = os.environ.get('X_ACCESS_SECRET', '')

          if not all([api_key, api_secret, access_token, access_secret]):
              print("X API credentials not set, skipping post")
              print(f"\nGenerated tweet:\n{tweet}")
              exit(0)

          auth = OAuth1(api_key, api_secret, access_token, access_secret)

          try:
              resp = requests.post(
                  'https://api.twitter.com/2/tweets',
                  auth=auth,
                  json={'text': tweet},
                  timeout=30
              )
              print(f"X API Response: {resp.status_code}")
              print(f"Response body: {resp.text}")
              if resp.status_code == 201:
                  print("Tweet posted successfully!")
              else:
                  print("Failed to post tweet")
          except Exception as e:
              print(f"X API error: {e}")
          SCRIPT
